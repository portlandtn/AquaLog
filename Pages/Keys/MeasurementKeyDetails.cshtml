@page "{measurementKeyId:int}"

@model MeasurementKeyDetails
@{
    ViewData["Title"] = "Measurement Key";
}
@if(Model.Message != null)
{
    <div class="alert- alert-info">@Model.Message</div>
}
<h1>@Model.MeasurementKey.Name</h1>
